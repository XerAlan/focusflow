# Assets

此目录用于存放应用图标。

- `icon.ico`：应用主图标（用于 Windows 资源管理器、安装包）。
  - 推荐尺寸：256x256
  - 格式：ICO

如果没有 `icon.ico`，`electron-builder` 会使用默认 Electron 图标。

你也可以使用在线工具（如 https://www.icoconvert.com/）把 PNG 转换成 ICO。
